/*
This is empty on purpose! Your code to build the resume will go here.
 */
 
/*Seletor jQuery p/ cabeçalho. #(cerquilha) indica que estamos procurando elemento com id cabeçalho*/
 $("#header").append("<h1><b>CURRÍCULO INTERATIVO</b></h1>");
 
 //Aparece nome na tela.
 $("#main").append("[Wanessa]");
 
  var formattedName=HTMLheaderName.replace("%data%", name);
 $("#header").append(formattedName);
 
 var role="Web Developer";
 var formatteRole=HTMLheaderRole.replace("%data%", role);
 
 $("header").prepend(formatteRole);
 $("#header").prepend(formattedName);
 
 <div>
 "Wanessa Costa"
 "Analysts Systems"
 </div>
 